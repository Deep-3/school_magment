<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Attendance Report</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 20px auto;
      border: 1px solid #ddd;
      border-radius: 5px;
      overflow: hidden;
    }
    .header {
      background-color: #4CAF50;
      color: white;
      text-align: center;
      padding: 10px;
    }
    .content {
      padding: 20px;
    }
    .footer {
      text-align: center;
      font-size: 0.9em;
      color: #777;
      padding: 10px;
      border-top: 1px solid #ddd;
    }
    .data-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    .data-table th,
    .data-table td {
      border: 1px solid #ddd;
      text-align: left;
      padding: 8px;
    }
    .data-table th {
      background-color: #f2f2f2;
    }
    .highlight {
      font-weight: bold;
      color: #4CAF50;
    }
    .highlight-low {
        font-weight: bold;
        color: #FF0000;
      }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2>Attendance Report</h2>
    </div>
    <div class="content">
      <p>Dear Parent,</p>
      <p>Below are the attendance details for your child:</p>
      
      <table class="data-table">
        <tr>
          <th>Student ID</th>
          <td>{{studentid}}</td>
        </tr>
        <tr>
          <th>Name</th>
          <td>{{name}}</td>
        </tr>
        <tr>
            <th>SubjectName</th>
            <td>{{subject}}</td>
          </tr>
        <tr>
          <th>Classes Attended</th>
          <td>{{attended_classes}}</td>
        </tr>
        <tr>
          <th>Total Classes</th>
          <td>{{total_classes}}</td>
        </tr>
        <tr>
            <th>Attendance Percentage</th>
          <td>
            {{#if (lt attendance_percentage 50)}}
            <span class="highlight-low">{{attendance_percentage}}%</span>
          {{else}}
            <span class="highlight">{{attendance_percentage}}%</span>
          {{/if}}
          </td>
       </tr>
      </table>

      <p>Regular attendance is crucial for academic success. Please ensure consistent attendance.</p>
      
      <p>Thank you for your attention.</p>
    </div>
    <div class="footer">
      <p>© Thank you</p>
    </div>
  </div>
</body>
</html>
